#!/bin/bash
# This script is executed as root by systemd on boot

echo "this is a modified tinfoil boot"
echo "hardcoded mount 32G of tmpfs in /mnt/ramdisk"

mount -t tmpfs -o size=32G tmpfs /mnt/ramdisk
chmod 777 /mnt/ramdisk

mount -t tmpfs -o size=1G tmpfs /tmp
